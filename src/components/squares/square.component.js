// @flow
import * as React from 'react';
import './style.css';

type Props = {
    value: String
};

type State = {};

class Square extends React.Component<Props, State> {
    render(): React.Node {
        return (
            <>

            </>
        );
    }
}

export default Square;